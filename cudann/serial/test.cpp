#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <iostream>

#include "linear.h"
#include "initialize.h"

int main(int argc, char *argv[]) {
    // Add layers
    int n_classes = 10;
    int n_features = 784;
    int n_batches = 2;
    double learning_rate = 0.01;

    // Input

    // Free memory
    free(input);

    return 0;
}

